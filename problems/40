select distinct orderdetails.orderid, productid, unitprice, quantity, discount from orderdetails                        join (                                                                                                                             select orderid from orderdetails where quantity >= 60 group by orderid, quantity having count(*) > 1                               ) ppo on ppo.orderid=orderdetails.orderid order by orderid, productid;
